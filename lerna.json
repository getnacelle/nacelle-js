{
  "packages": ["packages/*", "starters/*", "examples/*"],
  "version": "independent",
  "npmClient": "npm",
  "command": {
    "version": {
      "allowBranch": "main",
      "message": "chore(release): %s"
    },
    "publish": {
      "allowBranch": "main",
      "ignoreChanges": [
        "*.md",
        ".gitignore",
        "**/**/**.test.*",
        "**/.storybook/**"
      ]
    }
  }
}
